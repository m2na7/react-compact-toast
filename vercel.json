{
  "buildCommand": "pnpm install && pnpm run build:lib && cd playground && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "playground/.next",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs"
}